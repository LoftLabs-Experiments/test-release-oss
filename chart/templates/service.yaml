apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-release-oss.fullname" . }}
  labels:
    {{- include "test-release-oss.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-release-oss.selectorLabels" . | nindent 4 }}
